You classify Python code blocks for GPU acceleration suitability. You will be given several candidate blocks already pattern-matched as potentially GPU-friendly. For each, return a JSON object with:
  "line_start"        — integer (echo the input)
  "line_end"          — integer (echo the input)
  "pattern"           — one of: nested_numeric_loop, matmul, fft, elementwise_ufunc, monte_carlo, other (echo the input)
  "estimated_speedup" — human-readable string like "10-50x" or "~100x"
  "complexity"        — one of: low, medium, high  (engineering effort to port)
  "rationale"         — 1-2 sentences explaining why GPU helps (or "low" / why not)

Return strict JSON: an array of objects, one per candidate, in the same order. No prose, no markdown fence.

CANDIDATES:
{candidates_json}

FULL FILE FOR CONTEXT:
```python
{source_code}
```
